import {useFetch} from 'nuxt/app'
export const useTFetchTagsUidPostIdVariantIdOrdersRemove = <T extends {'uid':string;'postId':string;'variantId':string;}&{isActive:boolean;nextId:string;}&{age:number;itemId:string;}>(data: T) => useFetch<unknown>(`/api/tags/${encodeURIComponent(data.uid)}/${encodeURIComponent(data.postId)}/${encodeURIComponent(data.variantId)}/orders`, { method: 'delete', query:omit(data, 'uid','postId','variantId') })
export const useTFetchTagsUidPostIdVariantIdOrdersRemoveAsync = <T extends {'uid':string;'postId':string;'variantId':string;}&{isActive:boolean;nextId:string;}&{age:number;itemId:string;}>(data: T) => $fetch<unknown>(`/api/tags/${encodeURIComponent(data.uid)}/${encodeURIComponent(data.postId)}/${encodeURIComponent(data.variantId)}/orders`, { method: 'delete', query:omit(data, 'uid','postId','variantId') })
