import {useFetch} from 'nuxt/app'
export const useTFetchPidNamePostsRemoveByVariantId = <T extends ({'pid':string;'name':string;'variantId':string;}&{viewId:string;isActive:boolean;age:number;}|{nextId:string;outerId:string;})>(data: T) => useFetch<unknown>(`/api/${encodeURIComponent(data.pid)}/${encodeURIComponent(data.name)}/posts/${encodeURIComponent(data.variantId)}`, { method: 'delete', query:omit(data, 'pid', 'name','variantId') })
export const useTFetchPidNamePostsRemoveByVariantIdAsync = <T extends {'pid':string;'name':string;'variantId':string;}&{viewId:string;isActive:boolean;age:number;}|{nextId:string;outerId:string;}>(data: T) => $fetch<unknown>(`/api/${encodeURIComponent(data.pid)}/${encodeURIComponent(data.name)}/posts/${encodeURIComponent(data.variantId)}`, { method: 'delete', query:omit(data, 'pid','name','variantId') })
