import {useFetch} from 'nuxt/app'
export const useTFetchGetByPid = <T extends Omit<{nextId:string;title:string;email:string;age:number;productId:string;viewId:string;price:number;},'nextId'>&{'pid':string;}&Partial<{createdAt:string;isActive:boolean;}>>(data: T) => useFetch<unknown>(`/api/${encodeURIComponent(data.pid)}`, { method: 'get', query:omit(data, 'pid') })
export const useTFetchGetByPidAsync = <T extends Omit<{nextId:string;title:string;email:string;age:number;productId:string;viewId:string;price:number;},'nextId'>&{'pid':string;}&Partial<{createdAt:string;isActive:boolean;}>>(data: T) => $fetch<unknown>(`/api/${encodeURIComponent(data.pid)}`, { method: 'get', query:omit(data, 'pid') })
