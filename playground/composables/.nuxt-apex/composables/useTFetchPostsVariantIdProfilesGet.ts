import {useFetch,type UseFetchOptions} from 'nuxt/app'
export const useTFetchPostsVariantIdProfilesGet = <T extends unknown, R extends {data:string;}>(data: T, opt: UseFetchOptions<R> = {}) => useFetch<R>(`/api/posts/${encodeURIComponent(data.variant-id)}/profiles`, { method: 'get', query:omit(data, 'variant-id'), ...opt })
export const useTFetchPostsVariantIdProfilesGetAsync = <T extends unknown, R extends {data:string;}>(data: T, opt: UseFetchOptions<R> = {}) => $fetch<{data:string;}>(`/api/posts/${encodeURIComponent(data.variant-id)}/profiles`, { method: 'get', query:omit(data, 'variant-id'), ...opt })
